<%= simple_form_for(current_user) do |f| %>

  <%= f.hidden_field :fun_level, value: "100" %>

  <%= f.button :submit, "Arquemuses !", class: "answer next-card update_pill update_score text-center" %>
<% end %>